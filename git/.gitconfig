[user]
	name = Vladimir Bobes Tuzinsky
	email = vladimir@tuzinsky.com

[core]
	excludesfile = /home/bobes/.gitignore
	pager = less -FRSX
	whitespace=fix,-indent-with-non-tab,trailing-space,cr-at-eol

[color]
	diff = auto
	branch = auto
	interactive = auto
	pager = true
	status = auto
	ui = auto

[color]
	diff = auto
	branch = auto
	interactive = auto
	pager = true
	status = auto
	ui = auto
[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green
[color "diff"]
	meta = yellow
	frag = magenta
	old = red
	new = green
[color "status"]
	added = green
	changed = red
	untracked = cyan
